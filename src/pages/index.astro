---
// Layer 0 stub. Empty by design — the layered landing page is built in
// the next prompt; this scaffolding pass only verifies the toolchain.
import Base from "../layouts/Base.astro";
---

<Base />
