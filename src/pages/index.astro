---
import Base from "../layouts/Base.astro";
import { Lobby } from "../components/Lobby";

const title = "Quaestor";
const description =
  "The audit office for the machine economy. A non-custodial mandate engine and audit ledger for AI agent payments.";
---

<Base title={title} description={description}>
  <Lobby client:load />
</Base>
